{
  "name": "@attest/cli",
  "version": "1.0.0",
  "type": "module",
  "bin": {
    "attest": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "tsup": "^8.5.1",
    "vitest": "^4.1.7"
  },
  "dependencies": {
    "@attest/core": "workspace:*",
    "@attest/diff": "workspace:*",
    "@attest/runner": "workspace:*",
    "@attest/schema": "workspace:*",
    "clipanion": "^3.2.1"
  }
}
